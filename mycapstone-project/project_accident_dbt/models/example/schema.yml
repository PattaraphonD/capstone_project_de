
version: 2

models:
  - name: acc_sex_portion
    description: "accident case by sex portion (percentage)"
    columns:
      - name: acc_case_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
